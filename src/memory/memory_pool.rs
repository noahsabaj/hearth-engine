//! Memory Pool - Stub Implementation

pub struct MemoryPool;

impl MemoryPool {
    pub fn new() -> Self {
        Self
    }
}

impl Default for MemoryPool {
    fn default() -> Self {
        Self::new()
    }
}
